const { Role } = require('../models');

class RoleRepository {
  async findAll() {
    return [];
  }

  async findById(id) {
    return null;
  }

  async create(roleData) {
    return null;
  }
}

module.exports = new RoleRepository();
